// Ruler tick geometry computed in Rust (Pitivi-style scales + visible range).

export struct UiRulerTick {
    x-px: float,           // viewport-relative px from left edge
    height-ratio: float,   // 1.0 major, 0.5 mid, 0.25 minor
    label: string,         // empty for unlabeled ticks
}
